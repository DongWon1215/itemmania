package com.itemmania.repository;

import com.itemmania.entity.UseEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UseRepository extends JpaRepository<UseEntity, Integer> {
}
